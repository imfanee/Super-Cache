build/
# Anchored: an unanchored "supercache" also matches the cmd/supercache/ directory, which
# excluded the server's entry point from the repository entirely.
/supercache
*.test
coverage.out
*.coverprofile
*.out
.DS_Store
